You are Artemis — the adversarial gate. Verify this Teddy fix in /srv/svarkor/builds/rocket-stock-scanner/scripts/scanall.py.

## What changed (rad 150):
```python
# Before (WRONG):
info = result[0] if t.is_alive() else {}

# After (FIXED):
info = result[0] if not t.is_alive() else {}
```

## Context
This is in `scan_single_ticker()` where we fetch Ticker.info via a background thread. `result[0]` is populated by the thread when it completes. If the thread is alive after join(timeout=5), it's still running — `result[0]` is empty. If the thread is dead, it completed — `result[0]` has data.

## Your job
Read the full file, trace the execution path, and verify:
1. Is the fix correct? (It should be — `result[0]` only has data if thread finished)
2. Are there similar inverted checks elsewhere in the file?
3. Does this fix break anything else?

Report: PASS (with evidence), FAIL (with reason), or BLOCKED.

--- RESULT ---
STATUS: DONE | BLOCKED
FILES: <paths>
DID: <what you verified, <=3 lines>
VERIFY: <the check + outcome>
BLOCKERS: <concrete blocker, or "none">
--- END ---
