# Virtual environment
venv/
.env/
.env
.venv/

# Secrets (MC#939): real config lives ONLY in untracked env files.
# The committed template is config/scan_pro.env.example (placeholders only).
# The engine now lives under source/, so cover both locations.
config/*.env
!config/scan_pro.env.example
source/config/*.env
!source/config/scan_pro.env.example

# Python cache
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
*.egg

# pytest
.pytest_cache/
.coverage
htmlcov/

# Data files (local, not tracked) -- covered at repo root AND under source/ (engine moved there)
data/*.parquet
data/*.pkl
data/tickers/
data/ohlcv/
data/signals.db
data/*.db
data/*.csv
data/*.json
logs/
source/data/*.parquet
source/data/*.pkl
source/data/tickers/
source/data/ohlcv/
source/data/*.db
source/data/*.csv
source/data/*.json
source/logs/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Build ledger (internal tracking)
LEDGER.md
PLAN.md
teddy_prompt.txt

# Hermes tmp (fixed pattern: the stray files were named .hermes-tmp.*, removed from the served root)
.heroku-tmp*
.hermes-tmp*